import { Routes } from '@angular/router';
import { NewPizzaModalComponent } from './new-pizza-modal/new-pizza-modal.component';

export const routes: Routes = [
    {
        path: 'new-pizza',
        component: NewPizzaModalComponent
    }
];
